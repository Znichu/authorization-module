import React from 'react';

type Props = {};
export const Profile = (props: Props) => {
    return (
        <div>
            <div>
                <h3>Profile</h3>
            </div>
            <button >Logout</button>
        </div>
    );
};
